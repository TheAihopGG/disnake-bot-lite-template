# Переменные окружения
- `BOT_TOKEN`

- `DEBUG`

  Если равно 0, проект запустится с нормальном режиме. Если равно 1, проект запуститься с включенным дебаггером (debugpy).

- `POSTGRES_PASSWORD`

- `POSTGRES_USERNAME`

- `POSTGRES_DB`

- `PGADMIN_DEFAULT_EMAIL`

- `PGADMIN_DEFAULT_PASSWORD`
